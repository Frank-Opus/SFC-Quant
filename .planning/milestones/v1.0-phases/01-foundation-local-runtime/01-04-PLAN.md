---
phase: 01-foundation-local-runtime
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - README.md
  - backend/tests/test_health.py
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - OPS-01
must_haves:
  truths:
    - A new developer can follow documented bootstrap steps to start the backend and frontend locally.
    - The Phase 1 repo clearly explains safe defaults, env setup, and what is intentionally not implemented yet.
  artifacts:
    - path: README.md
      provides: Contributor-facing bootstrap and runtime guidance
    - path: backend/tests/test_health.py
      provides: Smoke verification for the documented backend startup path
  key_links:
    - from: README.md
      to: docker-compose.yml
      via: Documentation points to the exact one-command startup path the repo implements
    - from: README.md
      to: .env.example
      via: Documentation explains how to derive local env files from the committed example contract
    - from: README.md
      to: backend/tests/test_health.py
      via: Documentation and smoke verification refer to the same backend health behavior
---

<objective>
Document the bootstrap flow and verify the local runtime end to end.

Purpose: Close the phase by making startup understandable, reproducible, and explicitly safe for local use.
Output: README bootstrap guide and final smoke verification aligned with the actual compose/runtime behavior.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 1 bootstrap and safety guide</name>
  <files>README.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .env.example, docker-compose.yml, backend/app/main.py, frontend/src/App.tsx</read_first>
  <action>Create or rewrite the root `README.md` so it documents the Phase 1 purpose, prerequisites, environment setup, safe defaults, and local startup. Include concrete sections for `Overview`, `Tech Stack`, `Quick Start`, `Environment`, `Runtime Modes`, and `Current Phase Scope`. The quick start must show `cp .env.example .env` and `docker compose up --build`. The runtime explanation must explicitly say Phase 1 defaults to mock-safe startup and keeps live trading disabled.</action>
  <verify>test -f README.md && rg -n "Quick Start|docker compose up --build|mock-safe|live trading" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Quick Start`
    - `README.md` contains `docker compose up --build`
    - `README.md` contains `mock-safe`
    - `README.md` contains `live trading`
    - `README.md` contains `dSFC-Quant`
  </acceptance_criteria>
  <done>The repo documents a concrete safe bootstrap flow for a new developer.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize backend smoke verification around documented startup behavior</name>
  <files>backend/tests/test_health.py</files>
  <read_first>backend/tests/test_health.py, backend/app/main.py, backend/app/api/routes/health.py, backend/app/core/runtime.py, README.md</read_first>
  <action>Update `backend/tests/test_health.py` so it matches the final documented backend behavior. The smoke test must verify the app boots, `/health` returns HTTP 200, the response includes backend identity, and the resolved runtime mode indicates a safe non-live state when default env values are used. Keep the test narrow and Phase 1-specific; do not add execution, exchange, or AI workflow assertions.</action>
  <verify>cd backend && pytest -q</verify>
  <acceptance_criteria>
    - `backend/tests/test_health.py` contains `/health`
    - `backend/tests/test_health.py` asserts status code `200`
    - `backend/tests/test_health.py` asserts a runtime mode field or safe-mode indicator
  </acceptance_criteria>
  <done>Backend smoke verification matches the README and Phase 1 runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the documented local startup path against the actual repo artifacts</name>
  <files>README.md, .env.example, docker-compose.yml</files>
  <read_first>README.md, .env.example, docker-compose.yml, backend/Dockerfile, frontend/Dockerfile</read_first>
  <action>Cross-check the final Phase 1 docs against the implemented runtime files. Ensure the README references the actual service names `backend` and `frontend`, the actual ports `8000` and `5173`, and the actual `.env.example` keys. If any mismatch exists between documentation and implementation, fix the docs so the copy-paste startup flow matches the code exactly.</action>
  <verify>rg -n "backend|frontend|8000|5173|docker compose up --build" README.md docker-compose.yml .env.example</verify>
  <acceptance_criteria>
    - `README.md` references `backend` and `frontend`
    - `README.md` references `8000` or `/health`
    - `README.md` references `5173`
    - `README.md` references `.env.example`
  </acceptance_criteria>
  <done>Bootstrap documentation and runtime artifacts match exactly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest -q` succeeds
- [ ] `rg -n "docker compose up --build|mock-safe|backend|frontend" README.md` succeeds
- [ ] README, env contract, and compose file use matching service names and startup commands
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A new developer can follow the repo docs to start the Phase 1 stack safely
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-04-SUMMARY.md`
</output>
