---
phase: 01-foundation-local-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/api/__init__.py
  - backend/app/api/routes/__init__.py
  - backend/app/api/routes/health.py
  - backend/app/models/__init__.py
  - backend/tests/__init__.py
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - Developer can see a real backend app entrypoint and a real frontend app entrypoint in a single monorepo.
    - The repo has concrete backend and frontend workspace conventions that later phases can extend without restructuring the project.
  artifacts:
    - path: backend/app/main.py
      provides: FastAPI application entrypoint for local runtime
    - path: frontend/src/App.tsx
      provides: React application shell for local runtime
    - path: backend/pyproject.toml
      provides: Python package/runtime definition
    - path: frontend/package.json
      provides: Frontend workspace scripts and dependencies
  key_links:
    - from: backend/pyproject.toml
      to: backend/app/main.py
      via: Python project configuration exposes the app module used by local runtime commands
    - from: frontend/package.json
      to: frontend/src/App.tsx
      via: Frontend scripts boot the React shell from the workspace entrypoints
---

<objective>
Create the backend and frontend workspace skeletons for `dSFC-Quant`.

Purpose: Establish the repo/runtime shape early so later plans can add config, containers, and docs without revisiting directory structure.
Output: Runnable backend and frontend app shells with explicit workspace conventions.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the FastAPI backend workspace</name>
  <files>backend/pyproject.toml, backend/app/__init__.py, backend/app/main.py, backend/app/api/__init__.py, backend/app/api/routes/__init__.py, backend/app/api/routes/health.py, backend/app/models/__init__.py, backend/tests/__init__.py</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-local-runtime/01-CONTEXT.md, .planning/phases/01-foundation-local-runtime/01-RESEARCH.md</read_first>
  <action>Create a Python backend workspace under `backend/` using `pyproject.toml` and an `app/` package. Add a FastAPI application in `backend/app/main.py` that mounts a root route and a `/health` route from `backend/app/api/routes/health.py`. The root route should return JSON that includes `name: "dSFC-Quant"`, `service: "backend"`, and a Phase 1-safe runtime indicator. Keep the app shell thin: no agent graph, no execution logic, no exchange integration. Create package marker files and a `backend/tests/` package so later plans can add smoke checks without reorganizing the backend.</action>
  <verify>test -f backend/pyproject.toml && test -f backend/app/main.py && test -f backend/app/api/routes/health.py</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` exists
    - `backend/app/main.py` contains `FastAPI(`
    - `backend/app/api/routes/health.py` contains `@router.get("/health")`
    - `backend/app/main.py` contains `dSFC-Quant`
  </acceptance_criteria>
  <done>Backend workspace exists with a FastAPI app entrypoint and health route module.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the React frontend workspace</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/vite.config.ts, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-local-runtime/01-CONTEXT.md, .planning/phases/01-foundation-local-runtime/01-RESEARCH.md</read_first>
  <action>Create a Vite + React + TypeScript frontend workspace under `frontend/`. Define `dev`, `build`, and `preview` scripts in `frontend/package.json`. Add a custom app shell in `frontend/src/App.tsx` that explicitly frames the product as a future trading workstation instead of a generic admin template. Include a minimal connection/runtime panel or placeholder copy proving the frontend is custom and composable. Do not import a heavy admin/dashboard starter or copy a canned template structure. Add a single stylesheet file such as `frontend/src/styles.css` to establish baseline global styling.</action>
  <verify>test -f frontend/package.json && test -f frontend/src/App.tsx && test -f frontend/vite.config.ts</verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"dev"` and `"build"`
    - `frontend/src/App.tsx` exists
    - `frontend/src/App.tsx` contains `dSFC-Quant`
    - `frontend/src/App.tsx` does not contain `Admin`
  </acceptance_criteria>
  <done>Frontend workspace exists with Vite scripts and a custom React shell page.</done>
</task>

<task type="auto">
  <name>Task 3: Add root ignore rules for a two-workspace monorepo</name>
  <files>.gitignore</files>
  <read_first>.gitignore, backend/pyproject.toml, frontend/package.json</read_first>
  <action>Create or extend the root `.gitignore` so it ignores Python cache artifacts, virtual environments, frontend dependency/build directories, local `.env` files, and container/runtime clutter. Include concrete patterns for `__pycache__/`, `.venv/`, `node_modules/`, `dist/`, and `.env` while preserving `.env.example` files.</action>
  <verify>test -f .gitignore && rg -n "__pycache__/|node_modules/|dist/|\\.env$" .gitignore</verify>
  <acceptance_criteria>
    - `.gitignore` contains `__pycache__/`
    - `.gitignore` contains `node_modules/`
    - `.gitignore` contains `dist/`
    - `.gitignore` contains `.env`
  </acceptance_criteria>
  <done>Root ignore rules match the new backend/frontend workspace layout.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f backend/app/main.py && test -f frontend/src/App.tsx` succeeds
- [ ] `rg -n "FastAPI\\(|dSFC-Quant" backend frontend` returns expected scaffolding hits
- [ ] Backend and frontend workspaces exist under the repo root
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo contains explicit backend and frontend runtime shells for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md`
</output>
