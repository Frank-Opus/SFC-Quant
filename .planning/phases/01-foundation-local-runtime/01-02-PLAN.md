---
phase: 01-foundation-local-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .env.example
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/api/routes/system.py
  - backend/app/main.py
  - backend/tests/test_config.py
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "Developer can configure runtime behavior through `.env` values instead of editing code."
    - "Backend starts in a visible mock-safe mode when provider credentials are absent."
    - "Frontend shows the runtime mode so operators understand they are not in live trading."
  artifacts:
    - path: ".env.example"
      provides: "Root env contract for backend and frontend"
      contains: "APP_MODE=mock"
    - path: "backend/app/core/config.py"
      provides: "Typed settings loader and mock-safe readiness rules"
      contains: "mock_safe_mode"
    - path: "backend/app/api/routes/system.py"
      provides: "Runtime summary API route"
      exports:
        - "router"
    - path: "frontend/src/lib/api.ts"
      provides: "Frontend runtime summary fetch helper"
      min_lines: 10
  key_links:
    - from: ".env.example"
      to: "backend/app/core/config.py"
      via: "matching env field names"
      pattern: "APP_MODE|AI_PROVIDER|EXCHANGE_NAME|LIVE_TRADING_ENABLED"
    - from: "backend/app/api/routes/system.py"
      to: "frontend/src/lib/api.ts"
      via: "runtime summary fetch"
      pattern: "runtime"
    - from: "backend/app/main.py"
      to: "backend/app/api/routes/system.py"
      via: "router registration"
      pattern: "system"
---

<objective>
Add the Phase 1 configuration contract and explicit mock-safe runtime behavior.

Purpose: Satisfy PLAT-02 and PLAT-03 with one documented env story and backend-visible safe defaults, while keeping secrets server-side and future providers swappable.
Output: Root `.env.example`, typed backend settings, runtime summary route, tests for missing-credential behavior, and a frontend status surface fed by the backend.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
@.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the root env contract and backend mock-safe settings behavior</name>
  <files>.env.example, backend/app/core/__init__.py, backend/app/core/config.py, backend/app/api/routes/system.py, backend/app/main.py, backend/tests/test_config.py</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
backend/app/main.py
backend/app/api/routes/health.py
backend/tests/test_health.py
AGENTS.md
  </read_first>
  <behavior>
    - Test 1: Default settings resolve to `app_mode=mock`, `paper_trading_enabled=true`, and `live_trading_enabled=false` when secrets are unset.
    - Test 2: Runtime summary route reports readiness booleans without echoing secret values.
  </behavior>
  <action>Create a single root `.env.example` as the documented config source of truth. Include concrete keys: `APP_NAME=dSFC-Quant`, `APP_ENV=development`, `APP_MODE=mock`, `API_HOST=0.0.0.0`, `API_PORT=8000`, `FRONTEND_PORT=5173`, `FRONTEND_API_BASE_URL=http://localhost:8000`, `AI_PROVIDER=mock`, `AI_API_KEY=`, `EXCHANGE_NAME=mock`, `EXCHANGE_API_KEY=`, `EXCHANGE_API_SECRET=`, `PAPER_TRADING_ENABLED=true`, and `LIVE_TRADING_ENABLED=false`. Add typed backend settings in `backend/app/core/config.py` with computed booleans `ai_provider_ready`, `exchange_ready`, and `mock_safe_mode`. Register a `/api/v1/system/runtime` route that returns runtime flags and redacted readiness state, never secrets. Update `backend/app/main.py` to include the new route. Add tests that prove missing credentials still yield startup-safe defaults per PLAT-03.</action>
  <acceptance_criteria>
    - `rg -n "^APP_MODE=mock$|^AI_PROVIDER=mock$|^EXCHANGE_NAME=mock$|^PAPER_TRADING_ENABLED=true$|^LIVE_TRADING_ENABLED=false$" .env.example` returns matches.
    - `rg -n "mock_safe_mode|ai_provider_ready|exchange_ready|live_trading_enabled" backend/app/core/config.py` returns matches.
    - `rg -n "@router.get\\(\"/runtime\"|mock_safe_mode|paper_trading_enabled|live_trading_enabled" backend/app/api/routes/system.py` returns matches.
    - `rg -n "include_router" backend/app/main.py` shows the system router is mounted.
    - `rg -n "def test_default_settings_are_mock_safe|def test_runtime_summary_redacts_secrets" backend/tests/test_config.py` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest -q tests/test_health.py tests/test_config.py</automated>
  </verify>
  <done>Backend configuration is driven by environment values, mock-safe defaults are explicit and tested, and runtime state is inspectable through a dedicated API route.</done>
</task>

<task type="auto">
  <name>Task 2: Surface backend runtime mode in the frontend shell</name>
  <files>frontend/src/lib/api.ts, frontend/src/App.tsx</files>
  <read_first>
.planning/PROJECT.md
.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
frontend/src/App.tsx
backend/app/api/routes/system.py
AGENTS.md
  </read_first>
  <action>Add `frontend/src/lib/api.ts` with a small fetch helper for `/api/v1/system/runtime` that uses `import.meta.env.VITE_API_BASE_URL` or falls back to `http://localhost:8000`. Update `frontend/src/App.tsx` to request the runtime summary on load and render visible copy for `Mock Safe Mode`, `Paper Trading Enabled`, `Live Trading Disabled`, and provider readiness flags. If the backend is unavailable, render an explicit degraded-state message instead of failing silently. Do not expose raw secret values or add speculative dashboard widgets.</action>
  <acceptance_criteria>
    - `rg -n "system/runtime|VITE_API_BASE_URL|localhost:8000" frontend/src/lib/api.ts` returns matches.
    - `rg -n "Mock Safe Mode|Paper Trading Enabled|Live Trading Disabled|Backend Unreachable" frontend/src/App.tsx` returns matches.
    - `rg -n "fetchRuntime|useEffect|loading|error" frontend/src/App.tsx` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The frontend shell makes the backend runtime posture visible so operators can tell they are in a safe local mode before any trading features exist.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest -q tests/test_health.py tests/test_config.py`
- `cd frontend && npm run build`
</verification>

<success_criteria>
A developer can populate `.env` from the example file, backend behavior changes through settings instead of source edits, and missing AI or exchange credentials still result in a clear, safe runtime state that the frontend displays.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md`
</output>
