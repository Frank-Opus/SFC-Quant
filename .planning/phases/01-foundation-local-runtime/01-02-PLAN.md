---
phase: 01-foundation-local-runtime
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .env.example
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/core/runtime.py
  - backend/tests/test_health.py
  - frontend/src/lib/runtime.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - Developers can configure AI providers, exchange credentials, and runtime toggles from environment files without editing source code.
    - The stack starts in an explicit mock-safe mode when live secrets are absent.
  artifacts:
    - path: .env.example
      provides: Documented shared environment contract
    - path: backend/app/core/config.py
      provides: Backend settings loader for env-driven runtime
    - path: backend/app/core/runtime.py
      provides: Runtime mode resolution for mock-safe fallback
    - path: frontend/src/lib/runtime.ts
      provides: Frontend consumption of safe runtime metadata
  key_links:
    - from: .env.example
      to: backend/app/core/config.py
      via: Environment variable names documented at root are parsed by backend settings
    - from: backend/app/core/runtime.py
      to: backend/app/main.py
      via: FastAPI responses expose resolved safe mode instead of assuming live credentials
    - from: backend/app/main.py
      to: frontend/src/lib/runtime.ts
      via: Frontend shell can display backend runtime mode without hardcoding environment state
---

<objective>
Define the environment contract and mock-safe runtime behavior.

Purpose: Satisfy the phase requirement that configuration lives in `.env` files and the stack remains runnable without real exchange or AI credentials.
Output: Root environment example, backend settings loader, safe runtime resolver, and minimal verification of mock mode behavior.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a root `.env` contract for Phase 1</name>
  <files>.env.example</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-local-runtime/01-CONTEXT.md, .planning/phases/01-foundation-local-runtime/01-RESEARCH.md</read_first>
  <action>Create a root `.env.example` that documents concrete keys for backend/frontend coordination and safe defaults. Include at minimum `APP_ENV=development`, `APP_MODE=mock`, `LIVE_TRADING_ENABLED=false`, `AI_PROVIDER=mock`, `AI_API_KEY=`, `EXCHANGE_ID=binance`, `EXCHANGE_API_KEY=`, `EXCHANGE_API_SECRET=`, `FRONTEND_API_URL=http://backend:8000`, and `FRONTEND_WS_URL=ws://backend:8000/ws`. Keep secrets blank, keep safe-mode toggles explicit, and avoid any committed real credentials.</action>
  <verify>test -f .env.example && rg -n "APP_MODE=mock|LIVE_TRADING_ENABLED=false|AI_PROVIDER=mock|EXCHANGE_API_KEY=" .env.example</verify>
  <acceptance_criteria>
    - `.env.example` contains `APP_MODE=mock`
    - `.env.example` contains `LIVE_TRADING_ENABLED=false`
    - `.env.example` contains `AI_PROVIDER=mock`
    - `.env.example` contains `EXCHANGE_API_KEY=`
    - `.env.example` contains `FRONTEND_API_URL=http://backend:8000`
  </acceptance_criteria>
  <done>Root `.env.example` documents the exact safe-default runtime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement backend settings loading and mock-safe runtime resolution</name>
  <files>backend/app/core/__init__.py, backend/app/core/config.py, backend/app/core/runtime.py, backend/app/main.py</files>
  <read_first>backend/app/main.py, .env.example, .planning/phases/01-foundation-local-runtime/01-RESEARCH.md, .planning/phases/01-foundation-local-runtime/01-VALIDATION.md</read_first>
  <action>Add a backend settings module that reads environment variables from the Phase 1 contract and computes a resolved runtime state. Implement `backend/app/core/config.py` with typed settings for app mode, live trading enablement, AI provider, exchange ID, and optional credentials. Implement `backend/app/core/runtime.py` that resolves whether the application is in `mock-safe`, `paper-ready`, or `credentials-missing` startup state, with live mode forced off unless `LIVE_TRADING_ENABLED=true` and credentials are present. Update `backend/app/main.py` and `/health` responses so they include the resolved runtime mode and a clear `live_trading_enabled` boolean.</action>
  <verify>rg -n "APP_MODE|LIVE_TRADING_ENABLED|AI_PROVIDER|live_trading_enabled|mock-safe" backend/app</verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` contains `APP_MODE` or an equivalent settings field
    - `backend/app/core/runtime.py` contains `mock-safe`
    - `backend/app/main.py` or `backend/app/api/routes/health.py` contains `live_trading_enabled`
    - backend health or root response includes a runtime mode field
  </acceptance_criteria>
  <done>Backend settings and runtime metadata are fully env-driven and safe by default.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal smoke verification for mock-safe startup and expose runtime metadata to the frontend shell</name>
  <files>backend/tests/test_health.py, frontend/src/lib/runtime.ts, frontend/src/App.tsx</files>
  <read_first>backend/app/main.py, backend/app/core/config.py, backend/app/core/runtime.py, frontend/src/App.tsx, .env.example</read_first>
  <action>Create `backend/tests/test_health.py` to verify that the FastAPI app can start and that `/health` returns a JSON payload with the service name, runtime mode, and live-trading status in a safe default configuration. Add `frontend/src/lib/runtime.ts` with concrete types and fetch helpers or placeholder runtime metadata constants matching the backend response shape. Update `frontend/src/App.tsx` to display the runtime mode and explicitly indicate safe/mock startup when no credentials are configured.</action>
  <verify>test -f backend/tests/test_health.py && test -f frontend/src/lib/runtime.ts && rg -n "runtime|mock" frontend/src/App.tsx backend/tests/test_health.py</verify>
  <acceptance_criteria>
    - `backend/tests/test_health.py` exists
    - `backend/tests/test_health.py` contains `/health`
    - `frontend/src/lib/runtime.ts` exists
    - `frontend/src/App.tsx` contains `mock` or `safe`
  </acceptance_criteria>
  <done>Phase 1 has a concrete smoke test and frontend-visible mock-safe runtime state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "APP_MODE=mock|LIVE_TRADING_ENABLED=false" .env.example` succeeds
- [ ] `cd backend && pytest -q` succeeds
- [ ] `rg -n "runtime|mock-safe|live_trading_enabled" backend frontend` returns expected hits
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Configuration and fallback behavior are fully env-driven and safe by default
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md`
</output>
