---
phase: 01-foundation-local-runtime
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/Dockerfile
  - frontend/Dockerfile
  - docker-compose.yml
autonomous: true
requirements:
  - OPS-01
  - PLAT-01
must_haves:
  truths:
    - Developers can start backend and frontend together with a single documented `docker compose` command.
    - Compose defines exactly the backend and frontend services needed for the local-first baseline.
  artifacts:
    - path: backend/Dockerfile
      provides: Container build/runtime for FastAPI backend
    - path: frontend/Dockerfile
      provides: Container build/runtime for Vite frontend
    - path: docker-compose.yml
      provides: One-command local startup for the Phase 1 stack
  key_links:
    - from: docker-compose.yml
      to: backend/Dockerfile
      via: backend service builds from the backend workspace image definition
    - from: docker-compose.yml
      to: frontend/Dockerfile
      via: frontend service builds from the frontend workspace image definition
    - from: docker-compose.yml
      to: backend/app/api/routes/health.py
      via: backend healthcheck uses the app's health endpoint instead of a fake placeholder
---

<objective>
Containerize the baseline stack and define one-command local startup.

Purpose: Fulfill the local-first delivery promise with a deterministic backend/frontend compose flow.
Output: Backend Dockerfile, frontend Dockerfile, and a `docker-compose.yml` that boots both services with health plumbing.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a backend Docker image definition</name>
  <files>backend/Dockerfile</files>
  <read_first>backend/pyproject.toml, backend/app/main.py, backend/app/api/routes/health.py, .env.example</read_first>
  <action>Create `backend/Dockerfile` for a Python 3.12-based FastAPI runtime. Install backend dependencies from `backend/pyproject.toml`, copy the backend workspace, expose port `8000`, and define a concrete container command that runs the app with Uvicorn. Include a healthcheck that targets the app's `/health` endpoint, either directly in the Dockerfile or through Compose, using the real backend port and route.</action>
  <verify>test -f backend/Dockerfile && rg -n "8000|uvicorn|health" backend/Dockerfile</verify>
  <acceptance_criteria>
    - `backend/Dockerfile` contains `8000`
    - `backend/Dockerfile` contains `uvicorn`
    - `backend/Dockerfile` contains `HEALTHCHECK` or compose later provides backend healthcheck
  </acceptance_criteria>
  <done>Backend container definition can run the FastAPI app in Compose.</done>
</task>

<task type="auto">
  <name>Task 2: Create a frontend Docker image definition</name>
  <files>frontend/Dockerfile</files>
  <read_first>frontend/package.json, frontend/vite.config.ts, frontend/src/App.tsx, .env.example</read_first>
  <action>Create `frontend/Dockerfile` for the Vite frontend. Install frontend dependencies from `frontend/package.json`, copy the frontend workspace, expose port `5173`, and define a concrete command that serves the Phase 1 shell in a container-friendly way. Make sure the image can receive `FRONTEND_API_URL` and `FRONTEND_WS_URL` values from Compose or the frontend runtime strategy chosen for Phase 1.</action>
  <verify>test -f frontend/Dockerfile && rg -n "5173|npm|pnpm|yarn" frontend/Dockerfile</verify>
  <acceptance_criteria>
    - `frontend/Dockerfile` contains `5173`
    - `frontend/Dockerfile` contains a package manager install command
    - `frontend/Dockerfile` contains a command to start or serve the frontend app
  </acceptance_criteria>
  <done>Frontend container definition can serve the React shell in Compose.</done>
</task>

<task type="auto">
  <name>Task 3: Define a two-service `docker-compose.yml` with health plumbing</name>
  <files>docker-compose.yml</files>
  <read_first>backend/Dockerfile, frontend/Dockerfile, .env.example, backend/app/api/routes/health.py, .planning/PROJECT.md</read_first>
  <action>Create a root `docker-compose.yml` with exactly two primary services named `backend` and `frontend`. The `backend` service must build from `./backend`, expose `8000:8000`, load the Phase 1 env contract, and include a healthcheck against `/health`. The `frontend` service must build from `./frontend`, expose `5173:5173`, depend on the backend service, and receive concrete API/WS URLs pointing at the backend service name. Keep the compose stack lightweight: no database, no Redis, no third extra services in Phase 1.</action>
  <verify>test -f docker-compose.yml && docker compose config >/dev/null</verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `backend:`
    - `docker-compose.yml` contains `frontend:`
    - `docker-compose.yml` contains `8000:8000`
    - `docker-compose.yml` contains `5173:5173`
    - `docker-compose.yml` contains `healthcheck`
  </acceptance_criteria>
  <done>Compose can describe a valid two-service baseline stack with health plumbing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config >/dev/null` succeeds
- [ ] `rg -n "backend:|frontend:|healthcheck" docker-compose.yml` succeeds
- [ ] Dockerfiles expose the expected ports and runtime commands
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a deterministic two-service compose startup path
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-03-SUMMARY.md`
</output>
