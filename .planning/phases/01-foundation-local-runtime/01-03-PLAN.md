---
phase: 01-foundation-local-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docker-compose.yml
  - backend/Dockerfile
  - backend/.dockerignore
  - frontend/Dockerfile
  - frontend/.dockerignore
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Developer can boot backend and frontend with `docker compose up --build`."
    - "Compose runs only the two primary services required by project constraints."
    - "Container health checks reflect the backend health route and frontend HTTP availability."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service local startup topology"
      contains: "backend:"
    - path: "backend/Dockerfile"
      provides: "Backend container build recipe"
      contains: "uvicorn"
    - path: "frontend/Dockerfile"
      provides: "Frontend container build recipe"
      contains: "npm"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "backend build context"
      pattern: "build:"
    - from: "docker-compose.yml"
      to: "frontend/Dockerfile"
      via: "frontend build context"
      pattern: "build:"
    - from: "docker-compose.yml"
      to: "backend/app/api/routes/health.py"
      via: "healthcheck target /healthz"
      pattern: "healthz"
---

<objective>
Containerize the Phase 1 shell and make Docker Compose the default startup path.

Purpose: Fulfill OPS-01 with a deterministic two-service local topology that matches the project’s local-first constraint and Phase 1 success criteria.
Output: Backend and frontend Dockerfiles, `.dockerignore` files, and a `docker-compose.yml` that builds and health-checks both services.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
@.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
@.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend and frontend container build recipes</name>
  <files>backend/Dockerfile, backend/.dockerignore, frontend/Dockerfile, frontend/.dockerignore</files>
  <read_first>
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md
backend/pyproject.toml
backend/app/main.py
frontend/package.json
frontend/vite.config.ts
AGENTS.md
  </read_first>
  <action>Create a lean `backend/Dockerfile` based on Python 3.12 that installs backend dependencies and starts `uvicorn app.main:app --host 0.0.0.0 --port 8000`. Create a `frontend/Dockerfile` based on Node 20 that installs dependencies and starts the Vite app on `0.0.0.0:5173`. Add `.dockerignore` files that exclude `.git`, `.venv`, `__pycache__`, `.pytest_cache`, `dist`, and `node_modules`. Keep both images focused on local runtime, not production hardening or multi-service expansion.</action>
  <acceptance_criteria>
    - `rg -n "FROM python:3\\.12|uvicorn app\\.main:app --host 0\\.0\\.0\\.0 --port 8000" backend/Dockerfile` returns matches.
    - `rg -n "FROM node:20|npm run dev -- --host 0\\.0\\.0\\.0 --port 5173" frontend/Dockerfile` returns matches.
    - `rg -n "^node_modules$|^dist$|^\\.git$" frontend/.dockerignore` returns matches.
    - `rg -n "^__pycache__$|^\\.venv$|^\\.git$" backend/.dockerignore` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>docker build -f backend/Dockerfile backend && docker build -f frontend/Dockerfile frontend</automated>
  </verify>
  <done>Both workspaces have explicit container recipes and ignore rules suitable for local Compose startup.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the two-service compose topology with health checks and env loading</name>
  <files>docker-compose.yml</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md
docker-compose.yml
backend/Dockerfile
frontend/Dockerfile
backend/app/api/routes/health.py
.env.example
AGENTS.md
  </read_first>
  <action>Create a root `docker-compose.yml` with exactly two services named `backend` and `frontend`. Map ports `8000:8000` and `5173:5173`, load the root `.env` file, build from `./backend` and `./frontend`, and set health checks that hit `http://localhost:8000/healthz` for backend and `http://localhost:5173` for frontend. Use `depends_on` with `condition: service_healthy` so the frontend waits for backend readiness where supported. Do not add databases, brokers, or extra sidecars in Phase 1.</action>
  <acceptance_criteria>
    - `rg -n "^services:|^  backend:|^  frontend:" docker-compose.yml` returns exactly the two primary services.
    - `rg -n "8000:8000|5173:5173|env_file:|healthcheck:" docker-compose.yml` returns matches.
    - `rg -n "healthz|localhost:5173|condition: service_healthy" docker-compose.yml` returns matches.
    - `rg -n "postgres|redis|db:|broker:" docker-compose.yml` returns no extra service definitions.
  </acceptance_criteria>
  <verify>
    <automated>docker compose config >/dev/null</automated>
  </verify>
  <done>Running `docker compose up --build` has a valid two-service configuration aligned with the repo’s local-first constraints.</done>
</task>

</tasks>

<verification>
- `docker build -f backend/Dockerfile backend`
- `docker build -f frontend/Dockerfile frontend`
- `docker compose config >/dev/null`
</verification>

<success_criteria>
Compose is the default boot path, it uses only `backend` and `frontend`, and health checks point at the real Phase 1 shell instead of placeholder commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-03-SUMMARY.md`
</output>
