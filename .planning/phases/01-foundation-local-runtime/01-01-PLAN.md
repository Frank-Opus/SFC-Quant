---
phase: 01-foundation-local-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/api/__init__.py
  - backend/app/api/routes/__init__.py
  - backend/app/api/routes/health.py
  - backend/tests/test_health.py
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/components/app-shell.tsx
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - "Developer can start both workspace apps after dependencies are installed."
    - "Backend exposes a health route that responds without external credentials."
    - "Frontend renders a custom trading-workstation shell instead of a canned admin template."
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI application entrypoint with router registration"
      min_lines: 20
    - path: "backend/app/api/routes/health.py"
      provides: "Health endpoint for local runtime checks"
      exports:
        - "router"
    - path: "frontend/src/components/app-shell.tsx"
      provides: "Custom Phase 1 trading workstation shell"
      min_lines: 30
    - path: "backend/tests/test_health.py"
      provides: "Backend smoke coverage for app startup and health behavior"
      contains: "test_health"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/routes/health.py"
      via: "FastAPI include_router"
      pattern: "include_router"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "React root render"
      pattern: "createRoot"
    - from: "backend/tests/test_health.py"
      to: "backend/app/main.py"
      via: "TestClient app import"
      pattern: "from app.main import app"
---

<objective>
Create the initial monorepo application shells that later Phase 1 plans can build on.

Purpose: Establish the first real runtime conventions for `backend/` and `frontend/` without pulling in a template-heavy dashboard framework, per D-01 through D-03.
Output: Runnable FastAPI and Vite/React shells, a backend health endpoint, a custom frontend workstation shell, and minimal smoke-test/build scaffolding.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the backend control-plane shell and backend smoke test</name>
  <files>.gitignore, backend/pyproject.toml, backend/app/__init__.py, backend/app/main.py, backend/app/api/__init__.py, backend/app/api/routes/__init__.py, backend/app/api/routes/health.py, backend/tests/test_health.py</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/STATE.md
.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
AGENTS.md
  </read_first>
  <behavior>
    - Test 1: `GET /healthz` returns HTTP 200 with JSON keys `status`, `service`, and `version`.
    - Test 2: FastAPI app imports cleanly without exchange or AI credentials present.
  </behavior>
  <action>Create `backend/` as a Python 3.12 FastAPI workspace with a lean `pyproject.toml`, package layout under `backend/app/`, and a health route at `/healthz`. Keep the app intentionally thin: title it `dSFC-Quant API`, mount a versioned API router path (for example `/api/v1`), and expose a top-level health endpoint for container checks. Add `backend/tests/test_health.py` first so the executor can prove startup before adding more behavior. Update `.gitignore` with Python, Node, `.venv`, `.pytest_cache`, `node_modules`, `.env`, and build artifact ignores. Do not add PrimoAgent, Freqtrade, or exchange logic in this plan; this is only the runtime shell per D-03.</action>
  <acceptance_criteria>
    - `rg -n "name = \"dSFC-Quant\"|fastapi|uvicorn|pytest" backend/pyproject.toml` returns matches.
    - `rg -n "@router.get\\(\"/healthz\"|status|service|version" backend/app/api/routes/health.py` returns matches.
    - `rg -n "include_router|title=.*dSFC-Quant API" backend/app/main.py` returns matches.
    - `rg -n "def test_health|def test_app_imports_without_env" backend/tests/test_health.py` returns matches.
    - `rg -n "^\\.env$|^node_modules/$|^\\.venv/$" .gitignore` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest -q tests/test_health.py</automated>
  </verify>
  <done>The backend workspace boots via FastAPI, `/healthz` responds without secrets, and backend smoke coverage exists for startup and health behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the frontend workstation shell without a dashboard template</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/components/app-shell.tsx</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
AGENTS.md
  </read_first>
  <behavior>
    - Test 1: The frontend builds successfully with `npm run build`.
    - Test 2: The rendered shell includes explicit runtime sections such as backend status, execution mode, and upcoming agent roles.
  </behavior>
  <action>Create `frontend/` as a Vite + React + TypeScript workspace with a custom shell, not an imported admin starter, per D-01 and D-02. Use a dark trading-workstation direction with plain CSS/Tailwind-ready structure, but keep dependencies lean in Phase 1. The shell should render a branded header, a runtime-status panel, a placeholder market canvas area, and a roles/risk summary block so later dashboard phases have a clean expansion path. Include a `build` script in `frontend/package.json`; do not wire live WebSocket or chart dependencies yet.</action>
  <acceptance_criteria>
    - `rg -n "\"build\"\\s*:" frontend/package.json` returns a build script.
    - `rg -n "createRoot|StrictMode" frontend/src/main.tsx` returns matches.
    - `rg -n "dSFC-Quant|Runtime Status|Paper Trading Default|Risk Gate" frontend/src/App.tsx frontend/src/components/app-shell.tsx` returns matches.
    - `rg -n "background|font-family|color-scheme" frontend/src/index.css` returns matches showing custom shell styling.
    - `rg -n "admin|template" frontend/src/App.tsx frontend/src/components/app-shell.tsx` returns no framework-template branding.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm install && npm run build</automated>
  </verify>
  <done>The frontend workspace builds and renders a custom Phase 1 workstation shell that is compatible with the mandated frontend stack without locking the project into a canned dashboard.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest -q tests/test_health.py`
- `cd frontend && npm install && npm run build`
</verification>

<success_criteria>
Phase 1 has real `backend/` and `frontend/` applications on disk, backend health checks exist, frontend build tooling exists, and both shells reflect the project identity and constraints instead of placeholders or external templates.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md`
</output>
