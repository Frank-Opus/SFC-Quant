---
phase: 01-foundation-local-runtime
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - README.md
  - Makefile
  - scripts/bootstrap-check.sh
autonomous: true
requirements:
  - PLAT-01
  - OPS-01
must_haves:
  truths:
    - "Developer has one documented bootstrap flow for the full stack."
    - "Developer can verify local startup without guessing required commands."
    - "The documented flow makes mock-safe and paper-trading defaults explicit."
  artifacts:
    - path: "README.md"
      provides: "Bootstrap, env, and runtime usage documentation"
      contains: "docker compose up --build"
    - path: "Makefile"
      provides: "Optional lightweight aliases for local runtime"
      contains: "up:"
    - path: "scripts/bootstrap-check.sh"
      provides: "Automated end-to-end local startup verification"
      contains: "docker compose up --build -d"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "documented startup command"
      pattern: "docker compose up --build"
    - from: "scripts/bootstrap-check.sh"
      to: "backend/app/api/routes/health.py"
      via: "curl backend health route"
      pattern: "healthz"
    - from: "Makefile"
      to: "docker-compose.yml"
      via: "helper aliases"
      pattern: "docker compose"
---

<objective>
Document and verify the local bootstrap path end to end.

Purpose: Close the Phase 1 loop by giving developers a single reproducible startup flow and an executable verification script that proves the stack comes up in mock-safe local mode.
Output: Root README, lightweight Makefile aliases, and a bootstrap-check script that validates compose startup and service reachability.
</objective>

<execution_context>
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/workflows/execute-plan.md
@/home/wanguancheng/.AAAAAA/ai-quant-empire/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
@.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
@.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
@.planning/phases/01-foundation-local-runtime/01-01-SUMMARY.md
@.planning/phases/01-foundation-local-runtime/01-02-SUMMARY.md
@.planning/phases/01-foundation-local-runtime/01-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the root bootstrap guide and lightweight helper commands</name>
  <files>README.md, Makefile</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/phases/01-foundation-local-runtime/01-CONTEXT.md
.planning/phases/01-foundation-local-runtime/01-RESEARCH.md
.planning/phases/01-foundation-local-runtime/01-03-SUMMARY.md
docker-compose.yml
.env.example
AGENTS.md
  </read_first>
  <action>Create a root `README.md` that documents exactly one primary bootstrap path: copy `.env.example` to `.env`, then run `docker compose up --build`. Include backend URL `http://localhost:8000/healthz`, frontend URL `http://localhost:5173`, the meaning of `APP_MODE=mock`, and an explicit note that paper trading is enabled by default while live trading remains disabled. Add a lightweight `Makefile` with `up`, `down`, `logs`, and `bootstrap-check` targets that wrap the documented commands; keep it optional, not the canonical path.</action>
  <acceptance_criteria>
    - `rg -n "docker compose up --build|cp \\.env\\.example \\.env|http://localhost:8000/healthz|http://localhost:5173" README.md` returns matches.
    - `rg -n "mock-safe|paper trading|live trading" README.md` returns matches.
    - `rg -n "^up:|^down:|^logs:|^bootstrap-check:" Makefile` returns matches.
    - `rg -n "docker compose" Makefile` returns helper commands for each target.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "docker compose up --build|mock-safe|paper trading|live trading" README.md && make -n up down logs bootstrap-check</automated>
  </verify>
  <done>The repo documents a single bootstrap story clearly and provides lightweight aliases without competing with the canonical docker-compose flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add an executable end-to-end bootstrap verification script</name>
  <files>scripts/bootstrap-check.sh</files>
  <read_first>
.planning/phases/01-foundation-local-runtime/01-VALIDATION.md
.planning/phases/01-foundation-local-runtime/01-03-SUMMARY.md
docker-compose.yml
README.md
Makefile
AGENTS.md
  </read_first>
  <action>Create `scripts/bootstrap-check.sh` as a POSIX shell script that runs `docker compose config`, starts the stack with `docker compose up --build -d`, polls `http://localhost:8000/healthz` until it returns 200, checks `http://localhost:5173` for an HTTP 200, and always tears the stack down on exit. Make the script executable and keep log output concise so it can be reused by later verification workflows.</action>
  <acceptance_criteria>
    - `rg -n "docker compose config|docker compose up --build -d|docker compose down -v" scripts/bootstrap-check.sh` returns matches.
    - `rg -n "localhost:8000/healthz|localhost:5173" scripts/bootstrap-check.sh` returns matches.
    - `test -x scripts/bootstrap-check.sh` exits 0 after implementation.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/bootstrap-check.sh</automated>
  </verify>
  <done>An executor can prove the local stack starts and becomes reachable with one automated command, not just documentation claims.</done>
</task>

</tasks>

<verification>
- `rg -n "docker compose up --build|mock-safe|paper trading|live trading" README.md`
- `make -n up down logs bootstrap-check`
- `./scripts/bootstrap-check.sh`
</verification>

<success_criteria>
Phase 1 ends with a documented one-command startup path, explicit mock-safe messaging, and an automated bootstrap check that validates backend and frontend reachability through Docker Compose.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-runtime/01-04-SUMMARY.md`
</output>
